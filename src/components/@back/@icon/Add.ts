import styled from "styled-components";
import { ReactComponent as Back } from "../../../assets/arrow-left-line.svg";

const BackIcon = styled(Back)`
    width: 20px;
    cursor: pointer;
`

export default BackIcon;